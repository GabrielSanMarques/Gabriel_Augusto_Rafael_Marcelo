import { styled } from "../../Theme";

export const FramePrincipal = styled('div', {
    position: 'absolute',
    zIndex: 1,
    right: 0,
    width: '80%',
    maxWidth: '80%',
    minHeight: '100%',
    textAlign: 'center',
    backgroundColor: '$gray',
    boxShadow: '-5px 0px 10px 0px rgba(0, 0, 0, 0.2);',
    borderRadius: '15px 0 0 0 ',
    overflow: 'hidden',
});