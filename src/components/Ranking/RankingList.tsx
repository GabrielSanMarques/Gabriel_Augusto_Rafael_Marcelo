import { useEffect, useState } from "react";
import AuthService from "../../services/auth.service";
import UserService from "../../services/user.service";


export function UserInfo() {
    /*const ranking = UserService.getUserRanking();
    const [currentUser, setUser] = useState(AuthService.getCurrentUser());
    const [renderRanking, setRenderRanking] = useState(ranking.map(rankUser => <div>{rank.nome}</div>))
    const nomeUsuario = currentUser.nome;
    const emailUsuario = currentUser.email;*/


    return (
        <div>
            
        </div>       
    )
}